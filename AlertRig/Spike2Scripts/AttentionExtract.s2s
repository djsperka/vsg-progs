#include "AttentionExtractFunctions.s2s"

var hData%;
var hTxt;

' Get data file....
hData% := View(0);
if (ViewKind(hData%) <> 0) then
    Message("Current view must be a time view.");
'        return;
endif

hTxt := FileOpen(FileName$(1)+FileName$(2)+FileName$(3)+FileName$(4)+".txt", 8, 1);
View(hData%);
AttentionExtractInit(hTxt);
AttentionExtract%(-1);
FileClose(hTxt);
